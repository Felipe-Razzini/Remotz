<div class="project-grid">
  <p class="text-start"><strong>Name</strong></p>
  <p><strong>Start</strong></p>
  <p><strong>Due date</strong></p>
  <p><strong>Project Leader</strong></p>

  <% @projects.each do | project| %>
    <%# <div data-user-id="<%= project.user_id %>
      <p class="text-start text-primary"><strong><%= project.name%></strong></p>
      <p><%= project.start_date.strftime('%-d %b %Y') %></p>
      <p><%= project.end_date.strftime('%-d %b %Y') %></p>
      <p><%= project.user.username %></p>
    <%# </div> %>
  <% end %>

</div>
